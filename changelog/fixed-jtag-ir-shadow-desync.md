Fixed a JTAG state tracking bug where the host-side record of the current instruction register went stale after scan-chain interrogation and Test-Logic-Reset, causing register reads to skip the IR load and return the wrong data - visible as an all-zero RISC-V IDCODE from `probe-rs info`. The tracked instruction is now invalidated whenever its content cannot be known, and only a successful IR load marks it valid again.
